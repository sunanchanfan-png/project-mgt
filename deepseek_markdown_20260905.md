# สรุปบริบทโปรเจกต์: ระบบจัดทำรายงานความคืบหน้าประจำสัปดาห์ (Construction Weekly Report)

## ภาพรวม (Overview)
ระบบนี้เป็น Web Application ที่พัฒนาด้วย React (JavaScript/JSX) สำหรับโครงการก่อสร้าง เพื่อทดแทน/เสริมการทำงานของ TallyPrime เนื่องจาก TallyPrime ไม่มีฟังก์ชันสร้างหน้าปกและจัดลำดับหน้าแบบกำหนดเองได้ ระบบนี้จึงถูกพัฒนาให้ดึงข้อมูลจาก API มารวมและพรีวิวเป็น "เล่มรายงาน" หน้าตาเหมือนกระดาษ A4 ก่อนสั่งพิมพ์

## Tech Stack
- **Frontend:** React (Function Components + Hooks)
- **Styling:** CSS แบบมาตรฐาน (Custom CSS)
- **Data Fetching:** Axios (REST API)
- **การพิมพ์:** ใช้ `window.print()` ร่วมกับ CSS `@media print`

## ฟีเจอร์ที่พัฒนาและโครงสร้างหน้าพิมพ์ (ตามที่โจทย์ต้องการ)
ระบบถูกออกแบบให้แบ่งเป็นหน้า (Pages) ชัดเจนตาม `CompiledReportTab.jsx` และรองรับการพิมพ์ออกมาเป็น 7 หน้าหลักตามลำดับ:

1. **หน้าที่ 1 (Cover):** สร้างหน้าปก (ชื่อโปรเจกต์, Weekly Report #, วันที่) และจัดให้อยู่กลางหน้ากระดาษ (`report-print-page--cover`)
2. **หน้าที่ 2:** หัวข้อ "1. แผนงานและความคืบหน้างาน" (แสดง Overall Progress, กราฟ S-Curve และตารางกิจกรรมงานที่ทำในรอบสัปดาห์นี้)
3. **หน้าที่ 3:** หัวข้อ "ตารางสรุปปริมาณงานและผลงานรวมทั้งโครงการ" (แสดงตาราง Overall WBS)
4. **หน้าที่ 4:** รูปถ่ายความคืบหน้างาน (JE) แบ่งเป็นหน้าๆ อัตโนมัติ (สูงสุด 6 รูป/หน้า)
5. **หน้าที่ 5:** หัวข้อ "2. งานตรวจสอบคุณภาพ" และ "3. ความปลอดภัยและสิ่งแวดล้อม" (แบ่งกลุ่มรูปถ่ายสูงสุด 6 รูป/หน้า)
6. **หน้าที่ 6:** หัวข้อ "4. แผนงานสัปดาห์หน้า / Next week plan"
7. **หน้าที่ 7:** หัวข้อ "5. ปัญหาและอุปสรรค", "6. งานเพิ่ม/งานลด", "7. รายการที่รอการตัดสินใจ"

## Logic สำคัญในโค้ด
### 1. ฟังก์ชันแบ่งหน้าอัตโนมัติ (CompiledReportTab.jsx)
- ฟังก์ชัน `packEntriesIntoPrintPages(entries, maxPerPage)` ใช้จัดการการแบ่งกลุ่มรูปถ่ายให้อยู่เป็นหน้าเดียวกัน โดยจำกัดไม่เกิน 6 รูปต่อหน้า และกันไม่ให้ตัดรูปครึ่งกลางหน้า (ถ้ารวมแล้วเกิน 6 รูป ให้ทั้งหัวข้อเลื่อนไปขึ้นหน้าใหม่)

### 2. การโหลดข้อมูลแบบรวมศูนย์
- ใช้ `Promise.all` ใน `useEffect` เพื่อดึงข้อมูลจากหลายๆ Endpoint พร้อมกัน (Progress, Quality, Safety, Problems, Photos ฯลฯ) นำมาแสดงผลพร้อมกันในแท็บ "เล่มรายงาน"

### 3. การจัดการ CSS สำหรับการพิมพ์ (Reports.css)
- **`@media print`**: ซ่อนปุ่ม/แถบเมนูต่างๆ (Toolbar, Sidebar) ไม่ให้พิมพ์ติดไปกับกระดาษ
- **`@page`**: ตั้งค่าขนาดกระดาษ A4 และ Margin
- **`page-break-after: always`**: ใช้บังคับให้แต่ละ `div.report-print-page` ขึ้นหน้าใหม่เสมอ (ทำให้ ข้อ 1 ขึ้นหน้า 2, ข้อ 2 ขึ้นหน้า 3 ตามที่กำหนดไว้)
- **Cover Page Style:** ใช้ `padding-top: 80mm` ใน `@media print` เพื่อดันหน้าปกลงมาประมาณ 30% ของความสูงกระดาษ A4 (แทนการใช้ `height: 100vh` ที่ทำให้เกิดหน้าเปล่า)

## หมายเหตุการพัฒนา (จากคำถามก่อนหน้า)
- **ปัญหา TallyPrime:** เดิมทีผู้ใช้ต้องการให้ทำทั้งหมดใน TallyPrime แต่ทำไม่ได้ (ไม่มีฟีเจอร์สร้างหน้าปก/จัดหน้า) จึงตัดสินใจพัฒนา Web App นี้ขึ้นมาเพื่อสร้าง "เล่มรายงาน" แทน
- **การย้ายตาราง:** โค้ดปัจจุบันวาง "ตารางสรุปปริมาณงาน" ไว้เป็นหน้า 3 โดยอิสระ และวาง "กิจกรรมงานที่ทำในรอบสัปดาห์นี้" ไว้หน้า 2 ตามลำดับที่กำหนดใน JSX (สามารถสลับตำแหน่งได้โดยการย้าย `<div className="report-print-page">` ใน CompiledReportTab.jsx)

## สถานะปัจจุบัน
ระบบพร้อมใช้งานและสามารถสั่งพิมพ์เป็น PDF/กระดาษ A4 ได้ตามโครงสร้างแล้ว พร้อมสำหรับการแก้ไขต่อในอนาคต (เช่น การเปลี่ยนลำดับหน้า, เพิ่มโลโก้, ปรับขนาดฟอนต์ ฯลฯ)