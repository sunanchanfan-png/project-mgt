# SIKARIN — สรุปบริบทโปรเจกต์ ภาค 3 (ต่อจาก PROJECT_SUMMARY_PART2.md)

> ไฟล์นี้ต่อจาก `PROJECT_SUMMARY_PART2.md` ครอบคลุมตั้งแต่ทำ **Menu 5: จัดทำรายงาน** ให้เสร็จสมบูรณ์
> จนถึง **deploy ขึ้นเว็บจริง (GitHub/Railway/Vercel)**, ทำ **PWA**, และสร้าง **แอปมือถือแยกสำหรับ
> foreman** — เนื้อหาเยอะมาก ควรอ่านหัวข้อ "บั๊กที่เจอ" ให้ครบก่อนแก้ฟีเจอร์ที่เกี่ยวข้อง เพราะมีบั๊กแฝง
> หลายจุดที่ดูเผินๆ เหมือนถูกต้อง

---

## 1. Menu 5: จัดทำรายงาน (ทำเสร็จสมบูรณ์ในเซสชันนี้)

### โครงสร้าง 9 Tab
Container คือ `Reports.jsx` — **auto-provision** รายงานของสัปดาห์ปัจจุบันเสมอ (ไม่มีปุ่ม "สร้างรายงาน
ใหม่" อีกต่อไป) เรียก `GET /reports/current?project_id=X` ทุกครั้งที่เปลี่ยนโครงการ ถ้ายังไม่มีรายงาน
ของสัปดาห์นั้นจะสร้างให้อัตโนมัติ (ใช้ `ensureReportForWeek()` + UNIQUE constraint บน `(project_id,
week_start)` กัน race condition)

| Tab | Component | หน้าที่ |
|---|---|---|
| 1. Plan&Progress | `PlanProgressTab.jsx` | ตาราง WBS อ่านอย่างเดียว กรองเฉพาะกิจกรรมที่ %แผน>0 หรือ %actual>0 คำนวณ ณ วันที่ week_end ของรายงาน (ไม่ใช่วันนี้) |
| 2. ความปลอดภัย | `ReportItemsTab.jsx` (category='safety') | เพิ่ม/แก้/ลบรายการ + แนบรูปได้สูงสุด 6 รูป/รายการ |
| 3. รูปถ่าย | `PhotosTab.jsx` | ภาพรวมรูปทั้งหมด 2 คอลัมน์ (JE จาก Menu3 + ความปลอดภัย) คลิกเลือก/ยกเลิกเข้าเล่ม |
| 4. งานสัปดาห์หน้า | `NextWeekTab.jsx` | จัดกลุ่มตาม WBS Level1 จริง (ดึงชื่อจาก Menu2) |
| 5. ปัญหาอุปสรรค | `ReportItemsTab.jsx` (category='problems') | พิมพ์รายการเอง ไม่มีรูป |
| 6. งานเพิ่มลด | `ReportItemsTab.jsx` (category='additional_work') | พิมพ์รายการเอง ไม่มีรูป |
| 7. เรื่องที่ค้าง | `ReportItemsTab.jsx` (category='pending') | พิมพ์รายการเอง ไม่มีรูป |
| 9. เล่มรายงาน | `CompiledReportTab.jsx` | preview เต็มรูปแบบ + ปุ่ม Print + ดาวน์โหลด Word |

**หมายเหตุ:** เดิมมี Tab "คุณภาพงาน" (category='quality') อยู่ระหว่าง Tab1 กับ Tab2 ปัจจุบัน **ตัดออก
จากทั้งระบบแล้ว** (frontend + backend + menuRegistry) ตามคำขอ — ข้อมูลเก่าที่เคยมี category='quality'
ใน DB ยังอยู่ ไม่ได้ลบทิ้ง แค่ไม่มี endpoint ไหนเข้าถึงได้อีกแล้ว (ถ้าอยากลบข้อมูลเก่าออกจริงต้องเขียน SQL
เพิ่ม — ยังไม่ได้ทำ)

### รูปถ่าย — โมเดลสุดท้ายที่ยืนยันแล้ว (ผ่านการกลับไปกลับมาหลายรอบ)
**Pool + Select model** เหมือน Menu3 Tab1 (กิจกรรมงาน JE) ทุกประการ:
- แนบรูปเข้า "pool" ได้ไม่จำกัดจำนวน (ไม่บังคับ 6 รูปตอนแนบ)
- แต่ "เลือก" เข้าเล่มรายงานได้ไม่เกิน **6 รูป** ต่อรายการ (`MAX_PHOTOS_PER_ITEM = 6` ใน `routes/reports.js`)
- Tab3 "รูปถ่าย" คือที่ที่กดเลือก/ยกเลิกจริง (คลิกที่รูป toggle selection)
- Tab9 preview + ไฟล์ Word แสดงเฉพาะรูปที่ "เลือก" ไว้แล้วเท่านั้น

**⚠️ ระวัง:** ก่อนหน้านี้เคยลองทำเป็น "แนบ=เข้าเล่มอัตโนมัติ ไม่มีระบบเลือก" (เหมือน Menu3 ตอนแรกๆ) แล้ว
ก็ revert กลับมาเป็น pool+select อีกครั้ง — **อย่าเปลี่ยนกลับไปแบบเดิมอีกโดยไม่ถามยืนยันก่อน** เพราะเคย
สลับไปมาจนสับสนมาแล้วในเซสชันนี้เอง

### `MAX_PHOTOS` เปลี่ยนจาก 4 → 6 (ทำทีเดียวทั้งระบบ)
ไฟล์ที่แก้ (ต้องแก้พร้อมกันเสมอถ้าจะเปลี่ยนค่านี้อีก):
- Frontend: `WeeklyProgressTab.jsx`, `ReportItemsTab.jsx`, `PhotosTab.jsx`, `MobileForemanTab.jsx`,
  `ForemanSafetyTab.jsx`
- Backend: `routes/progress.js` (`MAX_PHOTOS`), `routes/reports.js` (`MAX_PHOTOS_PER_ITEM`)

### Tab9 "เล่มรายงาน" — โครงสร้างหน้า (ผ่านการปรับหลายรอบจนนิ่งแล้ว)

**จำนวนหน้า: 7 หน้าถ้า JE มีรูปที่เลือกไว้, 6 หน้าถ้าไม่มี** (ดูหัวข้อ 4 ด้านล่าง)

1. หน้าปก — Title+เส้นคั่นจัดตำแหน่ง ~30% จากขอบบน (ไม่ใช่กึ่งกลาง 50%)
2. แผนงานและความคืบหน้างาน — Overall box (Plan/Actual/Gain-Delay) + S-Curve รวมทั้งโครงการ + ตาราง
   กิจกรรมงานที่ทำในรอบสัปดาห์นี้
3. ตารางสรุปปริมาณงานและผลงานรวมทั้งโครงการ — **ไม่กรอง** โชว์ครบทุกกิจกรรมงาน (ต่างจาก Tab1 ที่กรอง
   เฉพาะ %>0) ใช้ endpoint ใหม่ `GET /reports/:id/progress-full` (ผ่าน `getReportProgressData(reportId,
   filterZeroActivities=false)`)
4. รูปถ่ายความคืบหน้า (JE) — **มีเงื่อนไขใหม่: ถ้า `photoGroups.length === 0` (ไม่มีรูปที่เลือกไว้เลย)
   ให้ข้ามหน้านี้ไปทั้งหน้า ไม่ใช่โชว์หน้าเปล่าที่เขียนว่า "ไม่มีรูปถ่าย"** — เหตุผล: งานช่วงออกแบบ/
   เอกสารมักไม่มีรูปถ่ายหน้างานเลย การบังคับมีหน้าเปล่าดูแปลก เลื่อนหน้าถัดไปขึ้นมาแทนดีกว่า ถ้ามีรูป
   แม้แต่กลุ่มเดียว logic เดิม (จำกัด 6 รูปต่อแผ่น, แบ่งหน้าอัตโนมัติผ่าน `packEntriesIntoPrintPages()`)
   ยังทำงานตามปกติทุกอย่าง
5. ความปลอดภัย — เลขลำดับ `1.) xxx` ถอย 22px (class `.report-preview__list--numbered`)
6. แผนงานสัปดาห์หน้า — **กรณีพิเศษ 2 ระดับ** (ดูหัวข้อเลขลำดับด้านล่าง)
7. ปัญหาอุปสรรค + งานเพิ่มลด + เรื่องที่ค้าง — เลขลำดับ `1.) xxx` ถอย 22px ทั้ง 3 หัวข้อ (last page,
   มี class `report-print-page--last`)

### กฎเลขลำดับใน Tab9 (สรุปให้ชัดเจนที่สุด — สับสนมาแล้วหลายรอบในเซสชันนี้)

มี **2 รูปแบบ** ตามลักษณะของ Tab:

**รูปแบบ A — "พิมพ์รายการเองตรงๆ ไม่ต้องเลือกอะไรก่อน"** (ความปลอดภัย, ปัญหาอุปสรรค, งานเพิ่มลด,
เรื่องที่ค้าง):
```
1.) กิจกรรมความปลอดภัยสัปดาห์นี้
2.) ตรวจสอบราวกันตกนั่งร้าน
```
ใช้ class `.report-preview__list--numbered` (`padding-left: 22px`, ไม่มี bullet เพราะเลขอยู่ในข้อความเอง)

**รูปแบบ B — "ต้องเลือกชื่องาน (WBS) ก่อนถึงจะเพิ่มได้"** (งานสัปดาห์หน้า เท่านั้น) — มี 2 ระดับจริง:
```
1.) JG-1 - ออกแบบ                          ← Level1 (ชื่องานที่เลือก) เลขลำดับ ถอย 22px
    • ส่ง draft รายการคำนวณและแบบให้ผู้ว่าจ้างอนุมัติ 70%   ← Level2 (พิมพ์เอง) bullet ถอย 44px
```
ใช้ class `.report-preview__nextweek-title` (22px, สำหรับ Level1) + `.report-preview__list--check`
(44px, bullet `•`, สำหรับ Level2 — **ไม่ใช่เลขลำดับ**)

**⚠️ เคยพลาดใส่เลขลำดับให้ Level2 ของงานสัปดาห์หน้าด้วย (ผิด — ตามภาพที่ผู้ใช้ส่งมา เห็นเลข 1.) ซ้อนกัน
2 ที่) ต้อง revert กลับมาเป็น bullet — ถ้าจะแก้เรื่องเลขลำดับใน Tab9 อีก ให้แยกให้ชัดว่ากำลังแก้ Tab
ไหน อย่าลืมว่างานสัปดาห์หน้าเป็นข้อยกเว้นที่มี 2 ระดับ**

**⚠️ อีกจุดที่พลาดมาแล้ว:** ตอนเพิ่มเลขลำดับให้ "ความปลอดภัย" (ข้อ 2) รอบแรก ใส่แค่เลขในข้อความแต่ลืม
เปลี่ยน CSS class ให้ตรง (ยังใช้ `.report-preview__list` เดิมที่มี `padding-left: 44px` ค้างอยู่) ทำให้
ระยะห่างไม่ตรงกับที่สั่ง (22px) ต้องแก้เพิ่ม class `--numbered` เข้าไปด้วย — **บทเรียน: เวลาใส่เลขลำดับ
ต้องเช็คทั้งข้อความ (เนื้อหา) และ class (ระยะห่าง) พร้อมกันเสมอ ไม่ใช่แค่แก้ข้อความอย่างเดียว**

**ต้องแก้คู่กันเสมอ:** `CompiledReportTab.jsx` (preview บนจอ) กับ `routes/reports.js` (ไฟล์ Word จริง)
— ทั้งเลขหัวข้อ, การเว้นระยะ (Word ใช้ `indent: { left: 440 }` ≈ 22px ที่ 20 twips/px), และเงื่อนไข
การแสดงผล (เช่นเงื่อนไข "ไม่มีรูป JE ข้ามหน้า" — จุดนี้ backend ไม่ต้องแก้เพราะ Word ออกแบบต่างจาก
Preview อยู่แล้ว ดูหัวข้อ 4)

---

## 2. Deploy จริง — GitHub + Railway + Vercel (ทำสำเร็จในเซสชันนี้)

**สรุปสถาปัตยกรรม:**
- GitHub: `sunanchanfan-png/project-mgt` (monorepo — backend/ + frontend/ ใน repo เดียว)
- Railway: backend (โปรเจกต์ชื่อ `secure-curiosity`, service ชื่อ `project-mgt`) — URL:
  `project-mgt-production.up.railway.app`
- Vercel: frontend — URL: `project-mgt-rouge.vercel.app`

**คู่มือเต็มอยู่ใน `DEPLOY_GUIDE.md`** (สร้างแยกไว้ต่างหาก ไม่ใช่ PART นี้) — สรุปย่อบั๊ก/ปัญหาที่เจอจริง
ระหว่าง deploy:

1. **CORS_ORIGIN / DATABASE_URL เป็นค่าตัวอย่างจาก `.env.example` ไม่ใช่ค่าจริง** — เจอ 2 รอบ (ครั้งแรก
   CORS_ORIGIN ยังเป็น `https://your-app.vercel.app`, ครั้งสอง DATABASE_URL ยังเป็น
   `postgresql://user:password@host:5432/dbname` ตัวอักษร "host" ที่เห็นใน error log
   `getaddrinfo ENOTFOUND host` คือหลักฐานชัดว่ายังไม่ได้แก้เป็นค่าจริง)

2. **Cloudinary API_SECRET ผิด** — error `Invalid Signature` ที่ Railway log แปลว่า credentials ไม่ตรง
   กับบัญชีจริง (ต้อง copy จาก Cloudinary Dashboard ตรงๆ ห้ามพิมพ์เอง เสี่ยงตัวอักษรตกหล่น)

3. **`vercel.json` ต้องนามสกุล `.json` เป๊ะ** — เคยเจอไฟล์ถูกสร้างเป็น `vercel.js` แทน (Windows ซ่อน
   นามสกุลไฟล์ ทำให้ผู้ใช้มองไม่เห็นว่าผิด) ทำให้ SPA routing ไม่ทำงาน (เข้าหน้าย่อยตรงๆ หรือ refresh
   ขึ้น 404) ต้องเช็ค Properties ของไฟล์ให้แน่ใจ

4. **Service Worker เก่าค้างใน browser หลัง deploy ใหม่** — ก่อนแก้ `skipWaiting`/`clientsClaim` (ดู
   หัวข้อ 3) เบราว์เซอร์ของผู้ใช้ที่เคยเข้าเว็บมาก่อนจะเห็น CSS/JS เก่าปนกับ HTML ใหม่ ("หน้าเว็บเละ") ทั้ง
   ที่ deploy ใหม่ไปแล้วจริง วิธีแก้ manual (ครั้งเดียวพอ ถ้า `skipWaiting`+`clientsClaim` deploy ไปแล้ว
   จะไม่เกิดอีก): DevTools → Application → Service Workers → Unregister → Storage → Clear site data

5. **Git push ครั้งแรกจากเครื่องผู้ใช้ตี rejected/non-fast-forward** — เพราะผู้ใช้ก็อปโค้ดจากเครื่องมา
   ให้ Claude ทำงานในสำเนาแยก แล้ว Claude เป็นคน push ขึ้น GitHub ก่อน พอผู้ใช้ทำ `git init` เองที่เครื่อง
   จริงทีหลัง ประวัติ git ไม่ตรงกัน ต้องใช้ `git push -u origin main --force` (เฉพาะตอน setup ครั้งแรก
   เท่านั้น ห้ามใช้ปกติ)

6. **Railway "+Add Service" เลือก repo ผิดตัว** — บัญชี GitHub มี repo เก่าชื่อ `HDL-Vending-MC` (คนละ
   โปรเจกต์) ปนอยู่ ทำให้ deploy ผิด repo ไปครั้งหนึ่ง (เห็น build error จาก Next.js/Nx ที่ไม่เกี่ยวข้อง
   เลย) วิธีแก้: ลบ service ผิดทิ้ง (ต้องพิมพ์ชื่อ service ยืนยันก่อนลบจริง) แล้วเพิ่มใหม่เลือก repo
   ให้ถูก

**บทเรียนรวม:** เกือบทุกปัญหา deploy เป็นเรื่อง **"ค่าตัวอย่างที่ยังไม่ได้แก้เป็นค่าจริง"** — เวลาบอกให้
ตั้งค่า environment variable ควรย้ำเสมอว่าอย่าเอาค่าจาก `.env.example` ไปใช้ตรงๆ

---

## 3. PWA (Progressive Web App) — สำหรับแอป foreman

**Phase 1 (เสร็จแล้ว):** ใช้ `vite-plugin-pwa` เพิ่ม manifest + Service Worker ให้ React app เดิมโดยไม่
ต้องเขียน Service Worker เอง:
- `vite.config.js` — `VitePWA({ registerType: 'autoUpdate', manifest: {...}, workbox: {
  globPatterns: [...], skipWaiting: true, clientsClaim: true } })`
- `main.jsx` — `registerSW({ immediate: true, onNeedRefresh: () => window.location.reload() })`
- ไอคอนแอป (`pwa-192.png`, `pwa-512.png`) — เป็นไอคอนชั่วคราว (ตัวอักษร "SK" พื้นส้ม สร้างด้วย Python
  PIL) เพราะไฟล์ favicon เดิมเป็นโลโก้ Claude ที่ติดมาจาก template ตอนสร้างโปรเจกต์ครั้งแรก **ยังรอ
  โลโก้จริงจากผู้ใช้อยู่**

**เหตุผลที่ต้องมี `skipWaiting`+`clientsClaim`:** ค่า default ของ workbox จะ "รอ" ให้ผู้ใช้ปิดแท็บเก่า
ทั้งหมดก่อนถึงจะสลับ Service Worker เวอร์ชันใหม่ ทำให้เจอปัญหา "หน้าเว็บเละ" ตามที่อธิบายในหัวข้อ 2 ข้อ 4
— ตั้งค่านี้ไปแล้วบังคับสลับทันทีไม่ต้องรอ

**Phase 2 (ยังไม่เริ่ม):** Offline support จริง (เก็บข้อมูลที่กรอกไว้ตอนไม่มีเน็ต แล้ว sync อัตโนมัติเมื่อ
มีเน็ตกลับมา) — วางแผนไว้แต่ยังไม่ได้ทำ เพราะ scope งานเปลี่ยนไปทำแอป foreman เต็มรูปแบบ (ระดับ 2) แทน

---

## 4. แอปมือถือแยกสำหรับ Foreman — "ระดับ 2" (แยก route, ใช้โค้ด/DB เดียวกัน)

### แนวคิดการตัดสินใจ (สำคัญ — อธิบายให้ผู้ใช้เข้าใจก่อนเริ่มทำจริง)
เสนอ 3 ระดับ: (1) โค้ดเดียว สลับ UI ตามขนาดจอ, (2) แยก route แต่ใช้โค้ด/DB เดียวกัน, (3) แอปแยกจริง
(React Native) — **ผู้ใช้เลือกระดับ 2** เพราะอยากให้ web เป็นหลัก (admin/pm/system_mgr เห็นทุกอย่าง)
แต่ foreman เห็นแค่ส่วนที่เกี่ยวกับเขา ไม่ต้องทำแอปแยกจริงให้ยุ่งยาก

### `/foreman` — route แยกเต็มรูปแบบ
`ForemanApp.jsx` — **ไม่มี Sidebar เลย** (component คนละตัวกับ `Layout.jsx` ที่ใช้ทั่วไป) มี Header เล็กๆ
+ ปุ่มออกจากระบบ + เลือกโครงการ + 4 Tab:

| Tab | Component | ข้อมูลผูกกับ |
|---|---|---|
| งานสัปดาห์นี้ | `MobileForemanTab.jsx` (week="this") | `progress_entries` (เดียวกับ Menu3) |
| งานสัปดาห์หน้า | `MobileForemanTab.jsx` (week="next") | `progress_entries` (เดียวกับ Menu3) |
| ความปลอดภัย | `ForemanSafetyTab.jsx` | `report_items` category='safety' (เดียวกับ Menu5 Tab2!) |
| S-Curve | `ForemanSCurveTab.jsx` | `/progress/scurve` (เดียวกับ Menu3 Tab4, อ่านอย่างเดียว) |

**`MobileForemanTab.jsx`** ใช้ร่วมกัน 2 ที่: (1) สลับมาจาก `ProjectManagement.jsx` (Menu3 PC) อัตโนมัติ
ถ้าจอแคบ **และ** role='foreman' เท่านั้น (role อื่นเห็นตาราง PC เสมอไม่ว่าจอกว้างแค่ไหน — ตัดสินใจแล้ว
ว่า system_mgr/admin/pm ต้องเห็นภาพรวมทั้งต้นไม้ WBS ได้ ไม่ใช่กรอกทีละกิจกรรมแบบ foreman) (2) เป็น Tab
"งานสัปดาห์นี้/หน้า" ใน `ForemanApp.jsx` โดยตรง

**หน้าจอ 2 แบบใน `MobileForemanTab.jsx`:**
- หน้าจอ 1 (รายการ): แบน WBS tree ให้เหลือแค่ array ของ JE (Level3) เดียว โชว์ %แผน/%ที่ทำแล้วเป็นการ์ด
  ใหญ่ กดง่าย — งานที่ `also_in_this_week=true` (คร่อม 2 สัปดาห์) กดไม่ได้ (ต้องแก้ที่ Tab สัปดาห์นี้)
- หน้าจอ 2 (กรอก): เห็นงานเดียวเต็มจอ มี slider ปรับ % ตัวใหญ่ + ปุ่มแนบรูป (≤6 รูป) + ปุ่มบันทึก

### `ForemanSafetyTab.jsx` — เพิ่มรายการความปลอดภัยจากมือถือ
ใช้ endpoint เดิมของ Menu5 ทั้งหมด (`GET/POST /reports/:id/items`, `POST/DELETE
/reports/items/:itemId/photos`) — ไม่มี backend endpoint ใหม่เลย ข้อมูลที่ foreman เพิ่มจากมือถือจะไป
โผล่ที่ Menu5 Tab2 (PC) ทันที เพราะเป็นตารางเดียวกัน (`report_items` category='safety')

**ต้องมี `reportId` ก่อนถึงจะเปิด Tab นี้ได้** — `ForemanApp.jsx` เรียก `GET /reports/current` เองตอน
เปลี่ยนโครงการ (คล้าย `Reports.jsx` ฝั่ง PC) เก็บ `reportId` ไว้ใน state แล้วส่งต่อให้
`ForemanSafetyTab.jsx`

### ProtectedRoute — บังคับ role='foreman' เข้าได้แค่ `/foreman`
```javascript
if (user.role === 'foreman' && !isForemanRoute) {
  return <Navigate to="/foreman" replace />;
}
```
เช็คจุดเดียวใน `ProtectedRoute.jsx` ครอบคลุมทุก route ที่เหลือทั้งหมดอัตโนมัติ (ไม่ต้องเพิ่ม
`roles={...}` ที่ทุก route ทีละอัน) — Login.jsx ก็ redirect ตาม role หลัง login สำเร็จ: `foreman` →
`/foreman`, อื่นๆ → `/dashboard`

---

## 5. บั๊กสำคัญที่เจอในเซสชันนี้ + วิธีแก้ (เรียงตามลำดับเวลา)

### 1. Login สำเร็จแต่เด้งกลับหน้า Login ทันที (บั๊กร้ายแรงที่สุดในเซสชันนี้)
**อาการ:** foreman login ผ่าน เห็นหน้า `/foreman` แวบเดียว แล้วเด้งกลับไป login ทันที
**สาเหตุที่แท้จริง:** `api/client.js` มี interceptor เดิมที่ตั้งใจไว้ว่า "401 หรือ 403 = token หมดอายุ
บังคับออกจากระบบทันที" — แต่ `ForemanApp.jsx` เรียก `GET /reports/current` ทันทีตอน mount ถ้า foreman
คนนั้นยังไม่มีสิทธิ์ Tab ไหนใน Menu5 เลย endpoint จะตอบ 403 กลับมา แล้ว interceptor ก็ลบ token ทิ้ง
ทันทีทั้งที่ login ถูกต้องจริงๆ
**แก้:** interceptor บังคับออกจากระบบเฉพาะ 401 เท่านั้น ปล่อย 403 ให้แต่ละหน้าจอ handle เอง (ดู pattern
14 ใน `CLAUDE.md`)

### 2. รูปหายหลังแก้ไข % (foreman มือถือ)
**อาการ:** foreman มีรูปแนบไว้ 4 รูป (ตอนนั้นยังเป็น limit 4) เปิดมาแก้แค่ % แล้วกดบันทึก (ไม่ได้แนบรูป
ใหม่) รูปทั้ง 4 หายหมด
**สาเหตุ:** `MobileActivityEntry` (ใน `MobileForemanTab.jsx`) เริ่ม state รูปจาก `[]` เปล่าๆ เสมอ ไม่เคย
โหลดรูปเดิมที่มีอยู่แล้ว พอกดบันทึกจึงส่ง `photo_urls: []` (ว่างเปล่า) ไป backend ซึ่ง backend ออกแบบไว้
ว่า "ลบรูปเก่าทิ้งก่อนเสมอแล้วค่อยใส่รูปใหม่ตามที่ส่งมา" (เพื่อรองรับกรณีอยากลบรูปทั้งหมดจริงๆ) ผลคือรูป
หายไปโดยไม่ตั้งใจ
**แก้:** preload รูปเดิมจาก `activity.photos` (ที่ backend ส่งมาให้อยู่แล้ว) เข้า state ก่อนเสมอ เหมือนที่
`WeeklyProgressTab.jsx` (PC) ทำอยู่แล้ว — เขียน `ForemanSafetyTab.jsx` ให้ถูกต้องตั้งแต่แรกด้วย pattern
เดียวกัน (ไม่พลาดซ้ำ)
**บทเรียนสำคัญ:** ตรวจสอบว่ามี "หน้าจอไหนบ้างที่แก้ไขรายการที่มีรูปแนบอยู่แล้วได้" (Menu3 PC, Menu5 PC,
foreman มือถือ ทั้ง 2 Tab) ก่อนสรุปว่าปัญหาแก้หมดแล้ว เพราะจุดที่มีบั๊กนี้อาจซ่อนอยู่หลายที่พร้อมกัน

### 3. Cloudinary อัปโหลดไม่สำเร็จ — "Invalid Signature"
ดูหัวข้อ 2 ข้อ 2 ด้านบน (เป็นปัญหา config ไม่ใช่โค้ด) — แต่ **ผลข้างเคียงที่ต้องระวัง:** ถ้า foreman เจอ
error อัปโหลดแล้ว**ยังกดปุ่มบันทึกต่อ** (ทั้งที่รูปยังไม่สำเร็จ) จะไปชนกับบั๊กข้อ 2 ซ้ำเติม (ยิ่งทำให้รูป
เดิมหายเร็วขึ้น) — ตอนนี้แก้บั๊กข้อ 2 แล้วทำให้ปัญหาซ้อนนี้จะไม่เกิดอีก แต่ต้นเหตุอัปโหลดพัง (credentials
ผิด) ต้องแก้แยกต่างหาก

### 4. หน้าจอเละหลังกด Ctrl+Shift+R ผ่านแล้ว แต่พอสลับเมนูกลับมาเละอีก
**อาการ:** hard refresh ครั้งแรกทำให้กลับมาปกติ แต่พอกดสลับเมนูอื่นแล้วกลับมา เละอีกครั้ง
**สาเหตุ:** ยังไม่ได้ push โค้ดที่แก้ Service Worker (`skipWaiting`/`clientsClaim`) ขึ้น GitHub จริง —
เบราว์เซอร์ที่เคย hard refresh ไปแล้วนั้น "บังเอิญ" ได้ของใหม่ตอน refresh ตรงนั้นเอง แต่ Service Worker
เก่ายังคุมอยู่ พอโหลดหน้าอื่นต่อก็กลับไปใช้ cache เก่าอีก
**แก้:** ต้อง push โค้ดจริงก่อนถึงจะหายขาด (ไม่ใช่แค่ hard refresh เฉยๆ)

### 5. Sidebar double-border (เห็นเป็นเส้น 2 เส้นซ้อนกัน)
**สาเหตุ:** ทั้ง `.reports-sticky-header` (กล่องครอบ) และ `.pdata-tabs` (แถบ tab ข้างใน) มี
`border-bottom` พร้อมกัน ระยะห่างกันแค่ 12px เลยดูเหมือนเส้นคู่ติดกัน
**แก้:** ลบ border ของกล่องครอบออก เหลือแค่เส้นใต้แถบ tab เส้นเดียว

### 6. Tab9 มือถือ — แถบซ่อน filter/tabs ยังเห็นเป็นแถบขาวทับตาราง
**สาเหตุ:** ตอนซ่อนเนื้อหา (filter/tabs/print) ไปแล้ว **ตัวกล่องครอบเอง** (`.reports-sticky-header`) ยัง
มี `background: #fff` + `padding` ค้างอยู่ กลายเป็นแถบขาวลอยทับซ้อนกับแถบ Title ของ Layout อีกชั้น
**แก้:** ตอนซ่อน (`headerCollapsed`) ต้องทำให้ตัวกล่องครอบเองโปร่งใส+ไม่มี padding ด้วย ไม่ใช่แค่ซ่อน
เนื้อหาข้างในอย่างเดียว

### 7. Tab9 หน้าปก — content ล้นสร้างหน้าเปล่าที่ 2 (ตอนพิมพ์จริง)
**สาเหตุ:** ใช้ `height: 100vh` กับหน้าปกตอนพิมพ์ ซึ่งเกินพื้นที่พิมพ์จริงของ A4 (297mm หักขอบบน-ล่างตาม
`@page` เหลือ ~267mm) เนื้อหาส่วนเกินไหลไปสร้างหน้าเปล่าที่ 2 โดยไม่ตั้งใจ
**แก้:** เอา `height: 100vh` ออก ปล่อยความสูงไหลตามเนื้อหาจริง ใช้ `padding-top: 80mm` (~30% ของพื้นที่
พิมพ์) แทนเพื่อจัดตำแหน่ง Title ให้อยู่ ~30% จากขอบบน

### 8. เส้นขอบบน table header หายตอนพิมพ์จริง (แต่เห็นปกติบนจอ)
**สาเหตุ:** `position: sticky` (ใส่ไว้ให้ header ค้างบนตอน scroll บนจอ) มีพฤติกรรมที่รู้กันว่าทำให้เส้น
ขอบบนหายตอนพิมพ์เมื่อผสมกับ `border-collapse`
**แก้:** ปิด sticky เฉพาะตอนพิมพ์ (`position: static !important` ใน `@media print`) เพราะพิมพ์จริงไม่มี
scroll อยู่แล้ว

### 9. `getReportProgressData` ต้องรองรับทั้งกรองและไม่กรอง (Tab1 vs Tab9 หน้า 3)
เพิ่มพารามิเตอร์ `filterZeroActivities` (default `true`) ให้ฟังก์ชันเดียวใช้ได้ทั้ง 2 แบบ แทนที่จะ
copy logic ซ้ำ — endpoint ใหม่ `GET /reports/:id/progress-full` เรียกด้วย `false`

**บทเรียนรวมของ session นี้:** บั๊กส่วนใหญ่เกิดจาก **"แก้จุดหนึ่งแล้วลืม sync จุดที่เกี่ยวข้อง"** — เช่น
preview กับ Word export, PC กับมือถือ, หรือ CSS ของกล่องครอบกับเนื้อหาข้างใน ต้องมี checklist เช็คจุด
ที่เกี่ยวข้องทั้งหมดทุกครั้งที่แก้ฟีเจอร์ที่มีหลายจุดแสดงผลข้อมูลชุดเดียวกัน

---

## 6. Known Limitations (ยังไม่ทำ/ตั้งใจเว้นไว้)

- **ไอคอน PWA เป็นไอคอนชั่วคราว** (ตัวอักษร "SK" พื้นส้ม) — รอโลโก้จริงจากผู้ใช้
- **Offline support (PWA Phase 2)** — ยังไม่ได้ทำ ยังไม่มี IndexedDB/background sync ใดๆ
- **ข้อมูลเก่า category='quality' ใน DB** — ยังไม่ได้ลบทิ้งจริง (แค่ตัด endpoint การเข้าถึง)
- **โลโก้บริษัทจริงในเล่มรายงาน/หน้าปก** — ยังใช้ Title ตัวหนังสือธรรมดา ไม่มีโลโก้ภาพ
- **Menu 4 การจัดการต้นทุน** — ยังไม่เริ่มเลย

---

## 7. งานถัดไป (สำคัญที่สุด — เริ่มจากตรงนี้ในเซสชันหน้า)

### 🎯 แอปมือถือสำหรับ "ลูกค้า" ดูความคืบหน้างานผ่านแอป

ผู้ใช้ต้องการให้ลูกค้า (เจ้าของโครงการ/ผู้ว่าจ้าง) เข้าดูความคืบหน้างานผ่านมือถือได้ — **ยังไม่ได้เริ่มทำ
เลย** ต้องคุยรายละเอียดกับผู้ใช้ก่อนเริ่มลงมือ (ตามธรรมเนียมของโปรเจกต์นี้ที่ต้องยืนยันความเข้าใจก่อนทำ
เสมอ) มีจุดที่ควรถามผู้ใช้ก่อนเริ่ม:

1. **Role ใหม่หรือใช้ role เดิม?** ระบบตอนนี้มี `admin, pm, foreman, viewer, system_mgr` — “ลูกค้า”
   ควรเป็น role ใหม่ (เช่น `client`) หรือใช้ `viewer` ที่มีอยู่แล้ว?
2. **ระดับการแยก (เหมือนที่ทำกับ foreman)?** แนะนำให้ทำ "ระดับ 2" แบบเดียวกับ foreman คือแยก route
   (`/client` หรือชื่ออื่น) ไม่มี Sidebar แต่ใช้โค้ด/DB เดียวกัน — จะ pattern เดียวกับ `ForemanApp.jsx`
   + `ProtectedRoute.jsx` (เพิ่ม `isClientRoute` คล้าย `isForemanRoute`)
3. **ลูกค้าควรเห็นอะไรบ้าง?** ต้องเป็น "อ่านอย่างเดียว" แน่นอน (ไม่ควรแก้ไขข้อมูลได้) — แต่ต้องถามว่า
   อยากเห็นแค่ Overall progress + S-Curve เฉยๆ หรืออยากเห็นรายละเอียดแบบ Tab9 เล่มรายงานด้วย (ถ้าอยาก
   เห็นรายงานเต็ม อาจจะแค่ทำหน้าจอมือถือที่ดึง `CompiledReportTab.jsx` logic มาแสดงแบบอ่านอย่างเดียว
   ก็พอ ไม่ต้องสร้างใหม่ทั้งหมด)
4. **1 ลูกค้าดูได้กี่โครงการ?** ถ้าลูกค้าคนหนึ่งเป็นเจ้าของหลายโครงการพร้อมกัน ต้องคิดเรื่อง filter
   โครงการที่ลูกค้าคนนั้นเข้าถึงได้ (อาจต้องเพิ่มตารางเชื่อม `project_id` กับ `user_id` ใหม่ ถ้ายังไม่มี
   กลไกนี้ในระบบสิทธิ์เดิม — `user_permissions` ตอนนี้ผูกกับ `menu_key/tab_key` เท่านั้น ไม่ได้ผูกกับ
   `project_id`)
5. **ต้องมี PWA ด้วยไหม?** โครงสร้าง PWA มีอยู่แล้ว (`vite-plugin-pwa` ติดตั้งแล้ว) น่าจะใช้ร่วมกันได้
   เลยไม่ต้องเพิ่มอะไรใหม่ตรงนี้

**แนะนำ:** เริ่มเซสชันหน้าด้วยการถามคำถามเหล่านี้ก่อน อย่ารีบเริ่มเขียนโค้ดทันที (ตามที่ผู้ใช้เน้นย้ำ
มาตลอดทั้งเซสชันนี้ว่าต้องยืนยันความเข้าใจให้ตรงกันก่อนเริ่มทำทุกครั้ง)
