# SIKARIN - Construction Project Management System
## สรุปบริบทโปรเจกต์ (Project Context Summary)

---

## 1. ภาพรวมระบบ

**สแตก:** React (Vite) + Node.js/Express + PostgreSQL (Supabase) + Cloudinary (แผนไว้ ยังไม่ได้ทำ)
**ภาษา UI:** ไทยทั้งหมด
**Auth:** JWT (เขียนเอง ไม่ใช้ Supabase Auth)
**โครงสร้างโปรเจกต์:**
```
C:\project-mgt\
├── backend\   (Node.js/Express, รันด้วย node src/server.js หรือ npm start)
│   ├── sql\   (ไฟล์ migration เรียงเลขตามลำดับ 001-008+)
│   └── src\
│       ├── db.js, server.js
│       ├── middleware/auth.js
│       └── routes\
└── frontend\  (React + Vite, รันด้วย npm run dev)
    └── src\
        ├── components\ (Layout, Sidebar, DateTextInput)
        ├── pages\
        │   ├── Login\
        │   ├── OpenProject\      (เมนู "เปิดโครงการ")
        │   └── ProjectData\      (เมนู "สร้างข้อมูลโครงการ")
```

---

## 2. Database: ย้ายไป Supabase แล้ว

ปัจจุบันฐานข้อมูลอยู่บน **Supabase** (โปรเจกต์ "Pgp" ใน org เดียวกับระบบอื่น) — ใช้ schema แยกชื่อ `project_mgt` เพื่อไม่ปนกับตารางของระบบอื่นในองค์กรเดียวกัน (Pgp มี ~50 ตารางใน schema `public`, HDL Vending อยู่อีกโปรเจกต์หนึ่ง)

**เหตุผลที่แยก schema:** Supabase Table Editor ใช้ schema เป็นตัวแบ่งกลุ่มตาราง (ไม่มีฟีเจอร์ folder จริง) — สลับ dropdown เลือก schema แล้วเห็นเฉพาะตารางของระบบนี้

**Connection:** ใช้ Transaction Pooler ของ Supabase (เหมาะกับ Node.js backend ที่เปิด-ปิด connection บ่อย) ตั้งค่าใน `backend/.env` → `DATABASE_URL`

**บั๊กสำคัญที่แก้ไปแล้วเรื่อง Database:**
- `pg` (node-postgres) มีค่าเริ่มต้นแปลงคอลัมน์ `DATE` เป็น JS Date object โดยใช้ **เวลาท้องถิ่นของเครื่อง backend** (ไม่ใช่ UTC) — เครื่องอยู่โซนเวลาไทย (UTC+7) ทำให้วันที่ถอยไป 1 วันเสมอตอนแปลงกลับเป็น UTC ส่งออกไป **แก้แล้วที่ `db.js`** ด้วย `types.setTypeParser(1082, val => val)` ให้ส่งเป็น string ธรรมดาแทน (แก้จุดเดียว ครอบคลุมทั้งระบบ)

---

## 3. โครงสร้างตาราง (Schema) ที่มีอยู่

```sql
project_mgt.users          -- ผู้ใช้งาน + role (admin/pm/foreman/viewer)
project_mgt.projects       -- ข้อมูลโครงการหลัก (รหัส SK-YYNN auto, มูลค่า, วันที่สัญญา, ฯลฯ)
project_mgt.wbs_level1     -- กลุ่มงานหลัก (JG-N) — มูลค่า, %หัก, is_final_group
project_mgt.wbs_level2     -- รายการงาน (JN-{กลุ่ม}{running}) — มูลค่า (ส่วนของกลุ่มงาน)
project_mgt.wbs_level3     -- กิจกรรมงาน (JE-{เลขจาก JN}-{running}) — มูลค่า, วันเริ่ม/จบ/จำนวนวัน
```

**หลักการคำนวณ Weight (ตกลงกันไว้ชัดเจน):**
- Level 1: `%Weight = มูลค่าเหลือ(กลุ่มนี้) ÷ ผลรวมมูลค่าเหลือทุกกลุ่ม × 100` (หารด้วยผลรวมของตัวเอง ไม่ใช่มูลค่าโครงการ ทำให้รวมได้ 100% เป๊ะเสมอ)
- Level 2: `%Share = มูลค่า ÷ มูลค่าเหลือของกลุ่มงานพ่อ × 100`, `%Weight = %Share × Weight ของกลุ่มงานพ่อ ÷ 100`
- Level 3: หลักการเดียวกัน คูณทบลงมาจาก Level 2
- ระยะเวลา (start/end date) กำหนดที่ **Level 3 เท่านั้น** ระดับบน roll-up จาก MIN/MAX ของลูกอัตโนมัติ

---

## 4. เมนูที่ทำเสร็จแล้ว

### 4.1 เปิดโครงการ (Menu 1)
- ตาราง list โครงการ + popup เพิ่ม/แก้ไข (รหัส SK-YYNN auto-run)
- ฟิลด์: ชื่อ, ผู้ว่าจ้าง, สัญญา, มูลค่า, **ระยะเวลา/วันเริ่ม/วันจบ (ผูกกันคำนวณอัตโนมัติ)**, ผู้ติดต่อ, สถานะ

### 4.2 สร้างข้อมูลโครงการ (Menu 2) — มี 4 Tab
1. **กลุ่มงานหลัก** — CRUD wbs_level1, checkbox "กลุ่มงานสุดท้าย" (ดึงมูลค่าที่ถูกหักจากกลุ่มอื่นกลับมา)
2. **รายการงาน** — CRUD wbs_level2, มุมมอง "-ทั้งหมด-" ดูทุกกลุ่มพร้อมกัน เช็คว่าแตกครบ 100% หรือยัง
3. **กิจกรรมงาน** — CRUD wbs_level3, dropdown 2 ชั้น (กลุ่มงาน→รายการงาน), มีวันที่/จำนวนวัน
4. **Gantt (ภาพรวม)** — read-only tree ทั้ง 3 ระดับ + กราฟ Gantt แบบลากกำหนดวันที่ได้จริง (บันทึกอัตโนมัติ), ซูมได้ 3 ระดับ (วัน/สัปดาห์/เดือน), เส้นอ้างอิงวันสิ้นสุดสัญญา, คลิกชื่อ JN เพิ่ม JE แบบด่วนได้ (quick-add ไม่ถามมูลค่า), แบนเนอร์เตือนค้างบนถ้ามีรายการงานแตกไม่ครบ 100%

**Design System ที่ล็อกไว้ใช้กับทุกเมนู:** ฟอนต์ 11px, header ตัวหนาจัดกลาง, ข้อมูลจัดกลาง (ยกเว้นคอลัมน์ชื่อ), เส้น grid เทาบางครบทุกช่อง, sticky header, คลิกแถวเปิด popup แก้ไขได้, popup กว้าง 450px ปุ่มบันทึก-ซ้าย/ปิด-ขวา

---

## 5. แผนพัฒนาเมนูถัดไป

| ลำดับ | เมนู | สถานะ |
|---|---|---|
| ✅ | เปิดโครงการ | เสร็จ |
| ✅ | สร้างข้อมูลโครงการ (WBS 3 ระดับ + Gantt Plan) | เสร็จ |
| ⏳ | **บันทึกความคืบหน้ารายวัน (Actual progress)** | ยังไม่เริ่ม — ต้องมีก่อนถึงจะทำ S-curve ได้ (ตาราง `progress_entries` ที่ออกแบบไว้ตั้งแต่ต้นบทสนทนา) |
| ⏳ | การจัดการโครงการ | ยังไม่เริ่ม |
| ⏳ | การจัดการต้นทุน | ยังไม่เริ่ม |
| ⏳ | จัดทำรายงาน | ยังไม่เริ่ม |
| ⏳ | การวิเคราะห์ (S-curve Plan vs Actual) | รอ Actual progress ก่อน |
| ⏳ | Milestone แยกสำหรับ % ที่ถูกหักไว้ (เช่น Asbuilt dwg) | พักไว้ตามที่ตกลง (เก็บ `deduct_percent` ไว้ในโครงสร้างแล้ว รอสร้างระบบ milestone ทีหลัง) |

---

## 6. ปัญหาที่ทำค้างไว้ล่าสุด (ยังไม่ได้รับการยืนยันว่าแก้ผ่าน)

**เรื่อง:** Scrollbar แนวนอนของตาราง Gantt อยู่ไกลเกินไปเวลามีหลายแถว ต้องเลื่อนจอลงไปสุดก่อนถึงจะเจอ

**ที่ลองทำไปแล้ว:**
1. รอบแรก: บังคับความสูงกล่อง Gantt ไว้ที่ `65vh` — **ทำให้แย่ลง** เกิดพื้นที่ว่างเปล่าขนาดใหญ่ระหว่างแถวสุดท้ายกับ scrollbar (โดยเฉพาะตอนมีข้อมูลน้อย)
2. รอบล่าสุด (ยังไม่ได้รับการยืนยันจากผู้ใช้): **ถอนการบังคับความสูงออก** เปลี่ยนเป็นทำ **แถบเลื่อนแนวนอนสำรอง** (`position: sticky; bottom: 0`) ลอยติดขอบล่างจอเสมอ sync กับตารางจริงแบบสองทางผ่าน `scrollLeft` — ยังไม่ได้ทดสอบว่าใช้งานได้ลื่นไหลจริงหรือไม่

**ไฟล์ที่เกี่ยวข้อง:** `frontend/src/pages/ProjectData/GanttView.jsx`, `GanttView.css`

**สิ่งที่ต้องทำต่อ:** รอ user ทดสอบและยืนยันผลของแถบเลื่อนสำรองตัวใหม่ ถ้ายังไม่ลื่นไหลอาจต้องพิจารณาวิธีอื่น เช่น ปุ่มลูกศรเลื่อนซ้าย-ขวาแทน หรือปรับความสูง/ตำแหน่งของแถบสำรองเพิ่มเติม
