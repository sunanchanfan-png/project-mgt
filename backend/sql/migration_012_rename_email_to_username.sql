-- ============================================
-- Migration 012: เปลี่ยนจาก email เป็น username ธรรมดา
-- ระบบนี้ไม่เคยส่งอีเมลจริงเลย (ไม่มี verify email, ไม่มี reset password ทางอีเมล) — คอลัมน์ email เดิม
-- ทำหน้าที่แค่เป็น "username ที่ไม่ซ้ำกัน" สำหรับ login เท่านั้น เปลี่ยนชื่อคอลัมน์ให้ตรงกับการใช้งานจริง
-- และไม่บังคับรูปแบบอีเมล (xxx@xxx.xxx) อีกต่อไป ผู้ใช้จะตั้งเป็นชื่ออะไรก็ได้ที่ไม่ซ้ำใคร
-- รันใน Supabase SQL Editor ตามลำดับก่อน deploy โค้ดที่พึ่งพา schema นี้ (ข้อมูล username เดิมที่เป็น
-- รูปแบบอีเมลอยู่แล้วจะยังคงค่าเดิมไว้ทุกประการ แค่เปลี่ยนชื่อคอลัมน์ ไม่กระทบข้อมูล)
-- ============================================

SET search_path TO project_mgt;

ALTER TABLE users RENAME COLUMN email TO username;
